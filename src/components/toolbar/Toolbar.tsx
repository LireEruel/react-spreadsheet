import { ToolbarContainer } from "./ToolbarStyles";
const Toolbar = () => {
  return (
    <ToolbarContainer>
      {/* 서식 설정 버튼들 */}
      <button>B</button>
      <button>I</button>
      <button>U</button>
      {/* 기타 툴바 아이템들 */}
    </ToolbarContainer>
  );
};

export default Toolbar;
